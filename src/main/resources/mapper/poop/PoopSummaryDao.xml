<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yuan.loveboot.poop.dao.PoopSummaryDao">

    <select id="selectUserWithMaxPoopCount" resultType="integer">
        SELECT user_id
        FROM poop_summary
        WHERE month = to_char(#{month}, 'YYYY-MM')
          AND is_deleted = false
          AND poop_count = (SELECT MAX(poop_count)
                            FROM poop_summary
                            WHERE month = to_char(#{month}, 'YYYY-MM')
                              AND is_deleted = false);
    </select>

    <select id="selectWithNicknameByMonth" resultType="com.yuan.loveboot.poop.vo.PoopSummaryVO">
        SELECT p.user_id, u.nickname, p.poop_count, p.is_winner
        FROM poop_summary p
        JOIN sys_user u ON p.user_id = u.id
        WHERE p.is_deleted = false
        <if test="start != null and end != null and start == end">
            p.month = to_date(#{startMonth}, 'YYYY-MM')
        </if>
        <if test=" start != null and start != end">
            AND p.month &gt;= to_date(#{startMonth}, 'YYYY-MM')
        </if>
        <if test=" start != null and start != end ">
            AND p.month &lt;= to_date(#{endMonth}, 'YYYY-MM')
        </if>;
    </select>
</mapper>
